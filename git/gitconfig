# Ceci est le fichier de configuration personnel de Git.
[user]
# Veuillez adapter et décommenter les lignes suivantes :
	name = Raphael Gallais-Pou
	email = rgallaispou@gmail.com
	signingkey = D412C2B5
[am]
	threeWay = true
[color]
	ui = auto
[commit]
	verbose = true
[core]
        abbrev = 12
	editor = vim
[diff]
	algorithm = histogram
	colorMoved = plain
	mnemonicPrefix = true
	renames = true
[merge]
	conflictstyle = zdiff3
[pretty]
        fixes = Fixes: %h (\"%s\")
	log = "%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset"
[rebase]
	autostash = true
[sendemail]
	confirm = always
	smtpServer = /usr/bin/msmtp
	suppressFrom = true
	suppresscc = self
[alias]
	au = "add --update"
	bv = "branch -vvv"
	c = "commit"
	ca = "commit --amend"
	co = "checkout"
	d = "diff"
	ds = "diff --staged"
	g = "grep -ni"
	la = "log --pretty=log --abbrev-commit --graph --all"
	ln = "log --pretty=log --abbrev-commit -n"
	ls = "log --pretty=log --abbrev-commit"
	pr = "pull --rebase"
	s = "status"
	sl = "stash list"
	sp = "stash push"
	spp = "stash pop"
	st = "stash show --text"
